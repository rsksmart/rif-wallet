import MaterialIcon from 'react-native-vector-icons/MaterialIcons'
import React from 'react'

const ScheduleIcon = (props?: object) => (
  <MaterialIcon name="schedule" {...props} />
)

export default ScheduleIcon
