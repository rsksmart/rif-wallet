import MaterialIcon from 'react-native-vector-icons/MaterialIcons'
import { MaterialIconInterface } from '.'

const WarningIcon = (props?: MaterialIconInterface) => (
  <MaterialIcon name="warning" {...props} />
)

export default WarningIcon
