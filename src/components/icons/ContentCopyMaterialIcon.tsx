import MaterialIcon from 'react-native-vector-icons/MaterialIcons'
import React from 'react'
import { MaterialIconInterface } from '.'

const ContentCopyMaterialIcon = (props?: MaterialIconInterface) => (
  <MaterialIcon name="content-copy" {...props} />
)

export default ContentCopyMaterialIcon
